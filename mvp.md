# MVP

## ゴール

ブラウザだけで NFC タグを読み書きできる Web ツール。
フロントエンドカンファレンス関西 2026 の登壇デモ受け皿であり、検索流入用の公開ツールでもある。

参考実装: https://github.com/illionillion/react-read-nfc-app

コンセプト名は持たせない。Web NFC のためのツールであることがそのままコンセプト。

## 技術

- Next.js（App Router）
- Tailwind CSS + cva + clsx
- デプロイ: Cloudflare Workers
- Web NFC（NDEF）は Client Component のみ
- 設定は Cookie に保存し、RSC から読み込む
- 履歴・書込下書きなど NFC データは localStorage（サーバーに送らない）

## ページ構成

| パス       | 内容                                                   |
| ---------- | ------------------------------------------------------ |
| `/`        | トップ（何のツールか・使い方概要・制約・CTA → `/app`） |
| `/app`     | 読み書きアプリ本体                                     |
| `/terms`   | 利用規約                                               |
| `/privacy` | プライバシーポリシー                                   |

## Must

### 対応・制約

- [ ] `NDEFReader` の有無で対応判定
- [ ] 非対応時の案内（Android Chrome / HTTPS が必要である旨）
- [ ] トップまたは `/app` に制約の明示

### 読取

- [ ] NDEF 読取（シリアル番号 + レコード一覧）
- [ ] レコード表示: text / url / json（mime `application/json`）
- [ ] 読取結果のクリップボードコピー
- [ ] 読取結果 → 書込への引き継ぎ（「この内容で書く」）

### 書込

- [ ] レコード種別: text / url / json
- [ ] 複数レコードの追加・編集・削除
- [ ] タグへの書き込み

### 消去

- [ ] タグ消去（空の NDEF メッセージで上書き）
- [ ] 実行前に確認ダイアログ（誤消去防止）

### localStorage

- [ ] 読取履歴（最大 **100件**、超過分は古いものから削除）
  - 保存内容例: 日時、シリアル番号、レコード内容
- [ ] 書込下書き（編集中のレコードセット）
- [ ] 履歴からの再表示・書込への再利用

### 設定（Cookie + RSC）

- [ ] ユーザー設定を Cookie に保存する
- [ ] App Router の RSC から Cookie を読み、初期表示に反映する
- [ ] 設定変更は Client から Cookie を更新し、必要なら再表示する
- [ ] MVP の設定項目: テーマ（light / dark / system）など、NFC 本体以外の軽い設定
- [ ] NFC の読取履歴・書込内容は Cookie に入れない

### SEO・公開ページ

- [ ] トップの title / description（Web NFC・読み書きが伝わる文言）
- [ ] 使い方の短い説明
- [ ] 利用規約・プライバシーポリシー

## Nice（MVP 後）

- [ ] PWA
- [ ] 書込テンプレ（名刺 URL など定型）
- [ ] AdSense
- [ ] OGP 画像

## Out of scope

- iOS 対応（Web NFC 非対応）
- アカウント・クラウド同期
- 宝探し・イベント用システム（別プロダクト）
- text / url / json 以外のレコード種別
- ネイティブアプリ

## 保存先の役割分担

| 保存先       | 用途                 | 読む場所                |
| ------------ | -------------------- | ----------------------- |
| Cookie       | テーマ等の設定       | RSC（初回 HTML に反映） |
| localStorage | 読取履歴・書込下書き | Client のみ             |

## 非機能

- HTTPS 必須（Web NFC の Secure Context）
- NFC の読取・書込・履歴は端末内完結（プライバシーポリシーに明記）
- Cookie は設定用途のみ（Secure / 適切な max-age）
- Cookie と localStorage の役割分担をプライバシーポリシーに明記

## 受け入れ条件（デモできる状態）

1. 対応 Android でタグを読める
2. text / url / json を書いて別端末（または再読取）で確認できる
3. 消去して再読取すると中身が空（または消えている）ことが分かる
4. 履歴が残り、リロード後も見える
5. 設定（テーマ等）が Cookie 経由で RSC 初期表示に反映される
6. PC / 非対応ブラウザでは理由が分かる
